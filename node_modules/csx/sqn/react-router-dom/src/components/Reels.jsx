import React from 'react'

// nested route component — renders inside Profile's <Outlet />
// matched when URL is /profile/:username/reels

function Reels() {
  return (
    <div>
      <p>reel1</p>
      <p>reel2</p>
      <p>reel3</p>
    </div>
  )
}

export default Reels
