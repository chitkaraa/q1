import React from 'react'

// nested route component — renders inside Profile's <Outlet />
// matched when URL is /profile/:username/photos

function Photos() {
  return (
    <div>
      <p>photo1</p>
      <p>photo2</p>
      <p>photo3</p>
    </div>
  )
}

export default Photos
